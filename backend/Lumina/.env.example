# Template for the local .env file. Copy this to `.env` and fill in real values.
# The real .env is gitignored; this example is committed so other devs know
# which secrets are required.
#
# Generate a key with:  openssl rand -base64 48
Jwt__Key="replace-with-a-strong-random-secret-at-least-32-chars"
